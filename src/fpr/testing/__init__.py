"""Fixture-generation helpers.

Everything in this sub-package exists to *create* protected sample files so the
removal path can be tested. It is never imported by the engine, the adapters,
the CLI or the GUI, and it contains no code that reads a user's files.
"""
